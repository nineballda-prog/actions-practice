name: Hello World

on:
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - name: チェックアウト
        uses: actions/checkout@v4

      - name: 挨拶を表示
        run: echo "Hello, GitHub Actions!"

      - name: 日時を表示
        run: date
